name: 
on:
  push:
jobs:
   hello-job:
      runs-on: ubuntu-latest
      steps:
         - name: checkot-code
           uses: actions/checkout@v3
         - name: print-message
           run: echo "hell"         
      
